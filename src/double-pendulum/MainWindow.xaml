<Window x:Class="double_pendulum.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:double_pendulum"
        xmlns:UserControls="clr-namespace:double_pendulum.view.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" MinWidth="600" MinHeight="300">
    <Grid Background="#FF1e1e1e">
        <!--Window can only have 1 single node. Grid is best choice.-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="60*"/>
            </Grid.RowDefinitions>

            <UserControls:Sliders Grid.Row="0" SliderColor="#2ba637" Quantity="L₁" Margin="10,0,0,0"/>
            <UserControls:Sliders Grid.Row="1" SliderColor="#2ba637" Quantity="L₂" Margin="10,0,0,0"/>
            <UserControls:Sliders Grid.Row="2" SliderColor="#2e60ab" Quantity="M₁" Margin="10,0,0,0"/>
            <UserControls:Sliders Grid.Row="3" SliderColor="#2e60ab" Quantity="M₂" Margin="10,0,0,0"/>

        </Grid>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="60*"/>
            </Grid.RowDefinitions>

            <UserControls:Sliders Grid.Row="0" SliderColor="#d62f4b" Quantity="D" Margin="0,0,10,0"/>
            <UserControls:Sliders Grid.Row="1" SliderColor="#e6de87" Quantity="θ₁" Margin="0,0,10,0"/>
            <UserControls:Sliders Grid.Row="2" SliderColor="#e6de87" Quantity="θ₂" Margin="0,0,10,0"/>

            <Viewbox Grid.Row="3" HorizontalAlignment="Left" Margin="10,0,0,5" StretchDirection="Both" Stretch="Uniform">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <CheckBox VerticalAlignment="Center"/>
                    <TextBlock VerticalAlignment="Center" Text="Show Trail" Foreground="White" FontSize="20" FontWeight="Bold" Margin="8,0,0,1"/>
                </StackPanel>
            </Viewbox>
            
        </Grid>

    </Grid>
</Window>
