<Window x:Class="double_pendulum.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:double_pendulum"
        xmlns:controls="clr-namespace:double_pendulum.Views.Controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" MinWidth="600" MinHeight="300"
        Focusable="True" PreviewMouseDown="Window_PreviewMouseDown">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="WhiteSmoke"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="BorderBrush" Value="WhiteSmoke"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="MinWidth" Value="150"/>
            <Setter Property="MaxWidth" Value="250"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="MaxHeight" Value="65"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="#FF1e1e1e">
        <!--Window can only have 1 single node. Grid is best choice.-->
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="60*"/>
            </Grid.RowDefinitions>
            <controls:QuantitySlider Grid.Row="0" Margin="10,0,0,0" SliderColor="#2ba637" Quantity="L₁" MinSliderValue="0" MaxSliderValue="10" QuantityValue="1"/>
            <controls:QuantitySlider Grid.Row="1" Margin="10,0,0,0" SliderColor="#2ba637" Quantity="L₂" MinSliderValue="0" MaxSliderValue="10" QuantityValue="1"/>
            <controls:QuantitySlider Grid.Row="2" Margin="10,0,0,0" SliderColor="#2e60ab" Quantity="M₁" MinSliderValue="0" MaxSliderValue="50" QuantityValue="10"/>
            <controls:QuantitySlider Grid.Row="3" Margin="10,0,0,0" SliderColor="#2e60ab" Quantity="M₂" MinSliderValue="0" MaxSliderValue="50" QuantityValue="10"/>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="80*"/>
            </Grid.RowDefinitions>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Button Name="StopButton" Margin="30,20,15,20" Background="Red" Content="Stop"/>
                <Button Grid.Column="1" Name="StartButton" Margin="15,20,30,20" Background="Green" Content="Start"/>
            </Grid>
            <Grid Grid.Row="1">
                <!-- Here goes pendulum animation -->
            </Grid>
        </Grid>

        <Grid Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="60*"/>
            </Grid.RowDefinitions>
            <controls:QuantitySlider Grid.Row="0" Margin="0,0,10,0" SliderColor="#d62f4b" Quantity="D" MinSliderValue="0" MaxSliderValue="10" QuantityValue="1"/>
            <controls:QuantitySlider Grid.Row="1" Margin="0,0,10,0" SliderColor="#e6de87" Quantity="θ₁" MinSliderValue="-180" MaxSliderValue="180" QuantityValue="30"/>
            <controls:QuantitySlider Grid.Row="2" Margin="0,0,10,0" SliderColor="#e6de87" Quantity="θ₂" MinSliderValue="-180" MaxSliderValue="180" QuantityValue="40"/>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,0,0,5">
                <CheckBox VerticalAlignment="Center"/>
                <TextBlock VerticalAlignment="Center" Text="Show Trail" Foreground="White" FontSize="20" FontWeight="Bold" Margin="8,0,0,1"/>
            </StackPanel>

        </Grid>

    </Grid>
</Window>
