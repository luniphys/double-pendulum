<UserControl x:Class="double_pendulum.Views.Controls.QuantitySlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:double_pendulum.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="QuantitySliderStyle" TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto" MinHeight="{TemplateBinding MinHeight}"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <!-- Track (Rail) -->
                            <Border Grid.Row="1" Height="4" Background="#555555" CornerRadius="2"
                                    VerticalAlignment="Center" Margin="5,0"/>
                            <Track Grid.Row="1" x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="Slider.DecreaseLarge" Style="{x:Null}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <!-- Filled portion of the rail -->
                                                <Border Height="4" Background="{Binding SliderColor, RelativeSource={RelativeSource AncestorType=UserControl}}" CornerRadius="2"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="Slider.IncreaseLarge" Style="{x:Null}">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Background="Transparent"/>
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <!-- Pointer (Thumb) -->
                                                <Ellipse Width="16" Height="16" Fill="WhiteSmoke"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center"
                   Background="#FF1e1e1e" Foreground="White"
                   FontSize="20" FontWeight="Bold" Text="{Binding Quantity, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        <Slider Name="Sliding"  Grid.Column="1" VerticalAlignment="Center" Margin="10,0,10,0"
                Style="{StaticResource QuantitySliderStyle}" 
                Minimum="{Binding MinSliderValue, RelativeSource={RelativeSource AncestorType=UserControl}}" Maximum="{Binding MaxSliderValue, RelativeSource={RelativeSource AncestorType=UserControl}}"
                Value="{Binding QuantityValue, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
        <TextBox Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center"
                 Background="#FF1e1e1e" Foreground="White" BorderThickness="0"
                 FontSize="20" FontWeight="Bold" Text="{Binding QuantityValue, RelativeSource={RelativeSource AncestorType=UserControl}}"/>
    </Grid>
</UserControl>
